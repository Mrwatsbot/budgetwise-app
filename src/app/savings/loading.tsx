import { AppShell } from '@/components/layout/app-shell';
import { SimpleLoading } from '@/components/layout/page-loading';

export default function Loading() {
  return (
    <AppShell>
      <SimpleLoading />
    </AppShell>
  );
}
