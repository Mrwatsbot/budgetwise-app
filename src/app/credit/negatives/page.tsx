'use client';

import { AppShell } from '@/components/layout/app-shell';
import { NegativeItemsContent } from '@/components/pages/negative-items-content';

export default function NegativeItemsPage() {
  return (
    <AppShell>
      <NegativeItemsContent />
    </AppShell>
  );
}
